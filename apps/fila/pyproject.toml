[project]
name = "suite-juviar-fila"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.118",
    "uvicorn[standard]>=0.37",
    "psycopg[binary]>=3.2",
    "pydantic-settings>=2.10",
]

[tool.ruff]
line-length = 100
